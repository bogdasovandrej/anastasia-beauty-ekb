# Чтобы в папку сайта случайно не попадали загрузки и временные файлы
*.exe
*.msi
*.zip
*.rar
*.docx
.codex_tmp_*
Thumbs.db
.DS_Store

# Ключи Cloudflare — лежат только на машине, в репозиторий не попадают
.cf-token
.dev.vars
.env
.env.*
.wrangler/
